FROM workcc/cuopt-scheduling:26.6

# 預設啟動 REST server（與官方 nvidia/cuopt image 相同行為）
# gRPC server：docker run -e CUOPT_SERVER_TYPE=grpc ...
# 互動模式：docker run --rm -it ... bash
CMD ["python", "-m", "cuopt_server.cuopt_service"]
